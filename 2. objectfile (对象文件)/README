1. 目标文件格式

1.1 文件格式
编译器编译源文件后生成的文件就做目标文件(*.o)。目标文件本质上已经是可执行文件格式，只是该文件中符号地址还没有经历重定位的过程。

目前PC平台目标文件格式都是COFF(Common File Format)的变种，如linux系统的ELF(Executable Linkable Format)和Windows平台的PE(Portable Executable)。对应的文件格式不仅被用来存储目标文件，还能够用来存储可执行文件、动态链接库(*.so)和静态链接库(*.a)。linux系统下使用file命令即可查看文件的类型。用列表表示如下：
ELF文件类型 -|- 说明
可重定位文件（relocatable file） -|- 包含数据和代码，可以被用来链接生成可执行文件，或共享库文件
可执行文件 (executable file) -|- 包含数据和代码，可以直接执行的程序，linux下一般没有后缀名
共享目标文件 (shared object file) -|- 包含数据和代码，使用方式有两种：静态链接，链接器将其和目标文件一起进行重定位；动态链接，动态链接器将其和可执行文件结合，作为进程映像的一部分来执行
核心转储文件 (core dump file) -|- 当进程意外终止时，系统将该进程地址空间的内容及进程终止时的一些信息转储到这类文件

1.2 目标文件内容
